# pnpm blocks dependency build scripts until each is explicitly approved.
# Both of these are toolchain packages with native binaries:
#   sharp         - Next.js image optimization
#   unrs-resolver - ESLint import resolver
allowBuilds:
  sharp: true
  unrs-resolver: true
